import { OnInit, ComponentFactoryResolver, ViewContainerRef, TemplateRef } from '@angular/core';
export declare class BlockUIDirective implements OnInit {
    private viewRef;
    private templateRef;
    private componentFactoryResolver;
    private blockUIComponentRef;
    blockTarget: string;
    message: string;
    template: any;
    blockUI: any;
    blockUIMessage: any;
    blockUITemplate: any;
    delayStart: any;
    delayStop: any;
    constructor(viewRef: ViewContainerRef, templateRef: TemplateRef<any>, componentFactoryResolver: ComponentFactoryResolver);
    ngOnInit(): void;
    private isComponentInTemplate(element);
    private findContentNode(element);
    private createComponent();
}
